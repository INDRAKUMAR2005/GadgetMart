server:
  port: 8086

spring:
  application:
    name: payment-service
  datasource:
    url: jdbc:postgresql://localhost:5432/gadgetmart_payments
    username: gadmun
    password: gm_password_2026
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# ──────────────────────────────────────────────────
# Razorpay Credentials — Replace with real keys from
# https://dashboard.razorpay.com/app/keys
# ──────────────────────────────────────────────────
razorpay:
  key-id: ${RAZORPAY_KEY_ID}
  key-secret: ${RAZORPAY_KEY_SECRET}
  currency: INR
  company-name: GadgetMart

kafka:
  bootstrap-servers: localhost:9092
